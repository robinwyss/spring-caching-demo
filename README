## Spring Caching Demo
This is a simple demo application, which uses the cache abstraction introduced in spring 3.1. The application uses the [openweathermap](http://openweathermap.org/) api to retrieve the weather for the current location. The weather data is then cached using the spring cache abstraction.
